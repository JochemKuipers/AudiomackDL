[project]
name = "audiomackdl"
version = "0.1.0"
description = "Download tracks/albums/playlists from Audiomack URLs via RapidAPI."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "python-dotenv>=1.2.2",
  "platformdirs>=4.9.6",
  "requests>=2.33.1",
  "tqdm>=4.67.3",
]
